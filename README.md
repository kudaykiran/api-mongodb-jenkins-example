# api-mongodb-jenkins-example

## Overview

This is an example of an environment built on Kubernetes to support an application with an API for documentation using Node.js + MongoDB + Jenkins for CI / CD.